<%= form_for(booking_request) do |f| %>
  <% if booking_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking_request.errors.count, "error") %> prohibited this booking_request from being saved:</h2>

      <ul>
      <% booking_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
